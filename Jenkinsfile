pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building for PROD environment ..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing in PROD environment ..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying in PROD ....'
            }
        }
    }
}
